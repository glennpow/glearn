---
  ### General Config

  ## start a tensorboard server
  tensorboard: true


  ### Debug Config

  ## log memory usage stats after each evaluation
  # debug_memory: true

  ## write activation summaries to tensorboard
  # debug_activations: true

  ## write gradient summaries to tensorboard
  # debug_gradients: true

  ## log session run calls
  # debug_runs: true

  ## session run tracing
  # debug_trace: true

  ## add numerics checks (NaNs, etc.)
  # debug_numerics: true


  ### Session Config

  session_config:
    gpu_options:
      allow_growth: true
    # log_device_placement: true
